<form method="post" action="{{ path('app_agence_delete', {'id': agence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agence.id) }}">
    <button class="btn">Delete</button>
</form>
